# Internal scoring model — 7 dimensions, must sum to 1.0
# Used for backend candidate ranking (FR-7.2)
scoring_weights:
  semantic_similarity: 0.25
  keyword_match: 0.15
  skill_match: 0.30
  experience_match: 0.15
  location_match: 0.05
  education_match: 0.05
  cross_encoder_score: 0.05

# UI-facing slider model — 6 dimensions, must sum to 1.0
# Used for interactive recruiter demo (FR-7.2a)
slider_weights:
  skill_match: 0.30
  experience_match: 0.25
  education_match: 0.15
  assessment_score: 0.15
  behavioral_signals: 0.10
  cultural_fit: 0.05

skill_importance_weights:
  required: 1.0
  preferred: 0.6
  nice_to_have: 0.3

proficiency_scores:
  beginner: 0.25
  intermediate: 0.50
  advanced: 0.75
  expert: 1.00

rrf_k: 60
max_replan_cycles: 3
min_good_matches_for_pass: 4

# Listwise tournament ranking (Plackett-Luce)
listwise_ranking:
  enabled: false
  group_size: 4
  max_em_iterations: 20
  num_tournament_rounds: 1
